{
	"name": "kirby-image-compare",
	"version": "1.0.1",
	"private": true,
	"scripts": {
		"dev": "kirbyup serve src/index.js",
		"build": "kirbyup src/index.js && node scripts/build-frontend.mjs",
		"check": "node scripts/build-frontend.mjs --check",
		"prepublishOnly": "npm run build"
	},
	"devDependencies": {
		"esbuild": "^0.24.2",
		"kirbyup": "^3.4.1"
	}
}
